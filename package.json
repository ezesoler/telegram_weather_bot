{
  "name": "bot_clima",
  "version": "1.0.0",
  "description": "Api que devuelve el clima de una region",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "EzeSoler",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2",
    "config": "^3.3.1",
    "moment": "^2.27.0"
  },
  "devDependencies": {
    "serverless-offline": "^6.5.0"
  }
}
